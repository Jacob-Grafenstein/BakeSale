{
  "name": "BakeSale",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "build": "babel src/ -d lib/",
    "prepublish": "npm run build",
    "flow":"flow; test $? -eq 0 -o $? -eq 2ß"
  },
  "dependencies": {
    "react": "16.2.0",
    "react-native": "0.52.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "22.1.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-react-native": "4.0.0",
    "flow-bin": "^0.64.0",
    "jest": "22.1.4",
    "react-test-renderer": "16.2.0"
  },
  "jest": {
    "preset": "react-native"
  }
}
